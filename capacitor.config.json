const config = {
  appId: 'mw.mzimba.edutrack',
  appName: 'MZ-Termly Report Card',
  webDir: '.',
  bundledWebRuntime: false,

  server: {
    url: 'https://e-emis.pages.dev',
    cleartext: false,
    androidScheme: 'https'
  },

  android: {
    allowMixedContent: false,
    webContentsDebuggingEnabled: false,
    useLegacyWebView: false
  },

  ios: {
    contentInset: 'automatic'
  },

  plugins: {
    PushNotifications: {
      presentationOptions: [
        'badge',
        'sound',
        'alert'
      ]
    },

    CapacitorHttp: {
      enabled: true
    }
  }
};

export default config;